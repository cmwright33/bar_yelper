<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <nav>
      <% if current_user.nil? %>
        <%= link_to "Sign up", sign_up_path %> or
        <%= link_to "Log in", log_in_path %>
      <% else %>
        <%= link_to "Neighborhoods", neighborhoods_path %></a> ||
        <%= link_to "Favorite Bars", favoritebar_index_path %></a> ||
        <%= link_to "Users", users_path%></a> ||
      <% end %>
      <% if current_user %>
          Logged in as <%= current_user.name %>.
          <%= link_to "Log out", log_out_path %> ||
      <% end %>
      </nav>
    </div>
  </div>
</header>